<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="purchase_order_line_wizard_form_discount_ths_base" model="ir.ui.view">
        <field name="name">purchase.order.line.wizard.form.discount.ths.base</field>
        <field name="model">purchase.order.discount</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="purchase_order_id" invisible="1"/>
                    <field name="company_id" invisible="1"/>
                    <field name="currency_id" invisible="1"/>
                    <div class="row">
                        <div class="col-sm-5 col-md-4 col-lg-4 col-4">
                            <group>
                                <label for="discount_amount" string="Discount" invisible="discount_type != 'fixed_amount'"/>
                                <field name="discount_amount" invisible="discount_type != 'fixed_amount'" nolabel="1"/>
                                <label for="discount_percentage" string="Discount" invisible="discount_type not in ('global_discount', 'line_discount')"/>
                                <field name="discount_percentage" invisible="discount_type not in ('global_discount', 'line_discount')" widget="percentage" nolabel="1"/>
                                <field name="tax_ids" invisible="1"/>
                            </group>
                        </div>
                        <div class="col-sm-7 col-md-8 col-lg-8 col-8">
                            <field name="discount_type" widget="radio"/>
                        </div>
                    </div>
                </sheet>
                <footer>
                    <button type="object" string="Apply" name="action_apply_discount" class="btn btn-primary" data-hotkey="q"/>
                    <button special="cancel" string="Discard" class="btn btn-secondary" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="account_move_line_wizard_form_discount_ths_base" model="ir.ui.view">
        <field name="name">account.move.line.wizard.form.discount.ths.base</field>
        <field name="model">account.move.discount</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="move_id" invisible="1"/>
                    <field name="company_id" invisible="1"/>
                    <field name="currency_id" invisible="1"/>
                    <div class="row">
                        <div class="col-sm-5 col-md-4 col-lg-4 col-4">
                            <group>
                                <label for="discount_amount" string="Discount" invisible="discount_type != 'fixed_amount'"/>
                                <field name="discount_amount" invisible="discount_type != 'fixed_amount'" nolabel="1"/>
                                <label for="discount_percentage" string="Discount" invisible="discount_type not in ('global_discount', 'line_discount')"/>
                                <field name="discount_percentage" invisible="discount_type not in ('global_discount', 'line_discount')" widget="percentage" nolabel="1"/>
                                <field name="tax_ids" invisible="1"/>
                            </group>
                        </div>
                        <div class="col-sm-7 col-md-8 col-lg-8 col-8">
                            <field name="discount_type" widget="radio"/>
                        </div>
                    </div>
                </sheet>
                <footer>
                    <button type="object" string="Apply" name="action_apply_discount" class="btn btn-primary" data-hotkey="q"/>
                    <button special="cancel" string="Discard" class="btn btn-secondary" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>